// ---------------------------------------------------------------------------
// Supabase client (placeholder for the design preview).
//
// This preview serves static sample data from lib/data.js. In the production
// rebuild, this file wires up Supabase and the pages fetch real records here.
//
//   1) npm install @supabase/supabase-js
//   2) add to .env.local:
//        NEXT_PUBLIC_SUPABASE_URL=...
//        NEXT_PUBLIC_SUPABASE_ANON_KEY=...
//   3) uncomment:
//
// import { createClient } from '@supabase/supabase-js'
// export const supabase = createClient(
//   process.env.NEXT_PUBLIC_SUPABASE_URL,
//   process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY
// )
//
// Then, for example:
//   const { data: leads } = await supabase.from('leads').select('*')
// with Row-Level Security enforcing which client each user can see.
// ---------------------------------------------------------------------------

export const supabase = null;
